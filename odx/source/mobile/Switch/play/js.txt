require([
  "dojo/dom",
  "dojo/ready",
  "dojox/mobile/Switch"
], function(dom, ready, Switch){
  ready(function(){
    var sw = new Switch({${props}}, "${id}");
    sw.placeAt(document.body);
    sw.startup();
  });
});