require([
    "dojo/_base/window",
    "dojo/ready",
    "dojox/mobile/Video",
    "dojox/mobile/parser",
    "dojox/mobile"
], function(win, ready, Video){
    ready(function(){
      var widget = Video({${props},source: [{src:"video/sample.mp4", type:"video/mp4"}]}, "${id}");
      win.body().appendChild(widget.domNode);
      widget.startup();
    });
});