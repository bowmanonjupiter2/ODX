require([
  "dojo/dom",
  "dojo/ready",
  "dojox/mobile/ProgressBar"
], function(dom, ready, ProgressBar){
  ready(function(){
    var pb = new ProgressBar({${props}}, "${id}");
    pb.placeAt(dom.byId("foo"));
    pb.startup();
  });
});