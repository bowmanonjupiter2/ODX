require([
    "dijit/form/VerticalSlider",
    "dijit/form/VerticalRule",
    "dojo/dom",
    "dojo/parser",
    "dojo/domReady!"
], function(VerticalSlider, VerticalRule, dom, parser){
    parser.parse();

    var rulesNode = document.createElement('div');
    ${id}.appendChild(rulesNode);
    var sliderRules = new VerticalRule({
        count: 11,
        style: "width:5px;"
    }, rulesNode).startup();

    var slider = new VerticalSlider(${props}, "${id}").startup();
});
