require(["dijit/Menu", "dijit/MenuItem", "dijit/form/ComboButton", "dojo/domReady!"],
        function(Menu, MenuItem, ComboButton){
    var menu = new Menu({ style: "display: none;"});
    var menuItem1 = new MenuItem({
        label: "Dojo",
        onClick: function(){ alert('Dojo'); }
    });
    menu.addChild(menuItem1);

    var menuItem2 = new MenuItem({
        label: "JQuery",
        onClick: function(){ alert('JQuery'); }
    });
    menu.addChild(menuItem2);
    menu.startup();

    var button = new ComboButton(${props},label:${label}, dropDown:menu);
    button.placeAt(dojo.body());
    button.startup();
});