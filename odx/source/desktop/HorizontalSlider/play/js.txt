require([
    "dojo/dom", 
    "dojo/parser", 
    "dijit/form/HorizontalSlider",
], function(dom, parser, HorizontalSlider)
    parser.parse();
    var slider = new HorizontalSlider(${props}, "${id}").startup();
});